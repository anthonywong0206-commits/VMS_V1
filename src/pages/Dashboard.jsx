import { Line, Pie } from 'react-chartjs-2';import { Chart as ChartJS, ArcElement, LineElement, PointElement, CategoryScale, LinearScale, Tooltip, Legend } from 'chart.js';import { calcHours, ranking } from '../utils/allowanceCalculator';
ChartJS.register(ArcElement,LineElement,PointElement,CategoryScale,LinearScale,Tooltip,Legend);
export default function Dashboard({data,month,setPage}){const s=calcHours(data.records,month);const ranks=ranking(data.records,data.volunteers,month);const months=[...new Set(data.records.map(r=>r.date.slice(0,7)))].sort();const monthTotals=months.map(m=>calcHours(data.records,m).total);const cards=[['本月服務時數',s.service,'小時'],['本月督導時數',s.supervision,'小時'],['本月個案服務時數',s.cases,'小時'],['本月小組服務時數',s.group,'小時'],['本月交通津貼',s.transport,'元']];return <div className="space-y-6"><div><h2 className="text-3xl font-black">首頁 Dashboard</h2><p className="text-slate-500">{month} 全體義工服務概況</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{cards.map(c=><div className="card p-5" key={c[0]}><p className="label">{c[0]}</p><div className="mt-2 text-3xl font-black">{c[1]}</div><p className="text-sm text-slate-500">{c[2]}</p></div>)}</div><div className="grid gap-6 xl:grid-cols-2"><div className="card p-5"><h3 className="mb-4 font-black">每月服務時數趨勢</h3><Line data={{labels:months,datasets:[{label:'總時數',data:monthTotals}]}} /></div><div className="card p-5"><h3 className="mb-4 font-black">服務類型分布</h3><Pie data={{labels:['小組服務','個案服務','督導','活動支援'],datasets:[{data:[s.group,s.cases,s.supervision,data.records.filter(r=>r.type==='活動支援').reduce((a,b)=>a+Number(b.hours||0),0)]}]}} /></div></div><div className="grid gap-6 xl:grid-cols-3"><div className="card p-5 xl:col-span-2"><h3 className="mb-4 font-black">義工排行榜</h3><table className="table w-full"><thead><tr><th>姓名</th><th>服務時數</th><th>督導時數</th><th>總時數</th></tr></thead><tbody>{ranks.map(r=><tr key={r.name}><td className="font-bold">{r.name}</td><td>{r.service}</td><td>{r.supervision}</td><td>{r.total}</td></tr>)}</tbody></table></div><div className="card p-5"><h3 className="mb-4 font-black">快速功能</h3><div className="grid gap-3"><button className="btn btn-primary" onClick={()=>setPage('activity')}>➕ 新增服務紀錄</button><button className="btn btn-soft" onClick={()=>setPage('case')}>📥 匯入 Excel</button><button className="btn btn-soft" onClick={()=>setPage('reports')}>📄 生成服務時數紀錄表</button><button className="btn btn-soft" onClick={()=>setPage('reports')}>💰 生成津貼簽收表</button><button className="btn btn-soft" onClick={()=>setPage('reports')}>📊 匯出月度報告</button></div></div></div></div>}
